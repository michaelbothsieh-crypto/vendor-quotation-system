# Task 1 Fix Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 還原標準 PostgreSQL 連線方式，調整欄位精度，改用標準 Next.js DB 單例，移除多餘依賴，並補齊測試腳本載入設定。

**Architecture:** 還原 Prisma postgresql provider 連線與 db.ts 的 global 單例，以防範連線洩漏並確保標準 PostgreSQL 驅動使用。

**Tech Stack:** Next.js, Prisma, PostgreSQL

---

### Task 1: 修改 schema.prisma 設定與欄位精度

**Files:**
- Modify: `prisma/schema.prisma:5-8`
- Modify: `prisma/schema.prisma:53-56`
- Modify: `prisma/schema.prisma:28-28`

**Step 1: 修改 schema.prisma 資料庫連線、欄位精度與註解**
將連線還原為 PostgreSQL Standard 連線，把四個 Float 工時欄位改為 Decimal(10, 1)，並將 status 註解改為繁體中文。

**Step 2: 執行驗證**
格式化 schema 檔：`npx prisma format`
預期：格式化成功，無語法錯誤。

**Step 3: Commit**
```bash
git add prisma/schema.prisma
git commit -m "refactor: adjust schema.prisma connection, decimal precision, and comment translation"
```

---

### Task 2: 移除 `src/lib/db.ts` 中的 pg Adapter

**Files:**
- Modify: `src/lib/db.ts:1-22`

**Step 1: 將 db.ts 還原成 global 單例連線**
移除 PrismaPg 與 pg 引入，完全使用標準 global 單例初始化 PrismaClient。

**Step 2: 執行驗證**
確認檔案編譯無語法錯誤。

**Step 3: Commit**
```bash
git add src/lib/db.ts
git commit -m "refactor: remove pg adapter from db.ts and restore standard client singleton"
```

---

### Task 4: 修改 `scripts/test-connection.ts` 補上 dotenv

**Files:**
- Modify: `scripts/test-connection.ts:1-1`

**Step 1: 在 scripts/test-connection.ts 首行加入 dotenv 載入**
加上 `import "dotenv/config";`。

**Step 2: Commit**
```bash
git add scripts/test-connection.ts
git commit -m "refactor: import dotenv/config in test-connection script"
```

---

### Task 3: 移除 `pg` 與 `@prisma/adapter-pg` 依賴

**Files:**
- Modify: `package.json:12-15`

**Step 1: 移除 package.json 中的 pg 及 @prisma/adapter-pg 依賴**
執行：`npm uninstall pg @prisma/adapter-pg @types/pg`

**Step 2: 整理依賴**
執行：`npm install`

**Step 3: Commit**
```bash
git add package.json package-lock.json
git commit -m "chore: uninstall pg and @prisma/adapter-pg dependencies"
```

---

### Task 5: 啟動資料庫並進行 Schema 同步與測試

**Files:**
- Modify: `C:/Users/Michael/.gemini/antigravity/scratch/vendor-quotation-system/.superpowers/sdd/task-1-report.md`

**Step 1: 啟動 Docker 容器**
執行：`docker compose up -d`

**Step 2: 執行 Schema 同步 (Migration)**
執行：`npx prisma migrate dev --name adjust_types_to_decimal`
執行：`npx prisma generate`

**Step 3: 測試資料庫連線**
執行：`npx tsx scripts/test-connection.ts`
預期輸出：「資料庫連線測試成功！」

**Step 4: 更新報告**
將修復進度與測試結果追加寫入報告檔案 `C:/Users/Michael/.gemini/antigravity/scratch/vendor-quotation-system/.superpowers/sdd/task-1-report.md`。

**Step 5: Commit**
```bash
git add .superpowers/sdd/task-1-report.md
git commit -m "docs: update task-1-report with fix details and test evidence"
```
